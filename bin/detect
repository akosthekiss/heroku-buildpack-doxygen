#!/usr/bin/env bash
# bin/detect BUILD_DIR

BUILD_DIR=$1

# this pack is valid for apps with Doxyfile in the root
if [ -f ${BUILD_DIR}/Doxyfile ]; then
    echo "Doxygen"
    exit 0
else
    exit 1
fi
